@inject ApplicationCore.Interfaces.IAsyncRepository<ApplicationCore.Entities.Product> repo

@{
    ViewData["Title"] = "Home Page";
}

<div class="row mt-3">
    @foreach (var item in await repo.ListAllAsync())
    {
        <p>@item.ProductName</p>
    }
</div>
